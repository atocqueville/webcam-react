export default class CheckboxItem {
    constructor(location, leftBox, rightBox, ack) {
        this.location = location;
        this.leftBox = leftBox;
        this.rightBox = rightBox;
        this.ack = ack;
    }
}